run:
  timeout: 5m
  go: '1.26'
linters:
  enable:
    - gofmt
    - goimports
    - misspell
    - gosec
    - bodyclose
    - errorlint
    - nilerr
    - unconvert
    - unparam
    - revive

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec